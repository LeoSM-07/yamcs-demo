# YAMCS Dashboard Demo

To get started clone the repo and run the following commands:

```bash
npm install && npm run dev
```

Also ensure that you have a local YAMCS instance running on `localhost:8090`
